const validateName = require('./validateName');
const validateDisease = require('./validateDisease');
const validateDate = require('./validateDate');
const validateDateInterval = require('./validateDateInterval');

module.exports = {
  validateName,
  validateDisease,
  validateDate,
  validateDateInterval
}